<%= simple_form_for(@cocktail) do |f| %>
    <%= f.input :name, label: 'Add New Cocktail', placeholder: "Mojito" %>
    <%= f.input :photo, as: :attachinary %>
    <%= f.button :submit, class: "btn btn-primary"  %>
<% end %>
